server:
  port: 8081
jwt:
  secret: my-super-secret-key
  expiration: 3600000
spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/qma_auth
    username: root
    password: AnushkaSinghal24

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka